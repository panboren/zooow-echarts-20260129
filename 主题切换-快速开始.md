# 🎨 图表主题切换功能 - 快速开始

## ✨ 新功能亮点

- ✅ **8种精美主题** - 从经典到赛博朋克，满足各种视觉需求
- ✅ **一键切换** - 点击即可更改所有46个图表的配色
- ✅ **实时预览** - 主题切换器中显示颜色预览
- ✅ **自动保存** - 主题选择持久化存储
- ✅ **零侵入** - 无需修改现有图表组件

## 🚀 快速使用

### 1. 启动项目

```bash
npm run dev
```

### 2. 查看主题切换器

打开浏览器，页面右上角会看到主题切换按钮：
- 按钮显示当前主题名称
- 点击打开下拉菜单
- 菜单显示所有可用主题及颜色预览

### 3. 切换主题

点击任意主题，所有图表立即应用新配色！

## 📋 可用主题列表

| 主题名称 | 描述 | 适用场景 |
|---------|------|---------|
| 🎨 默认主题 | 经典蓝紫色渐变 | 通用场景 |
| 🌙 暗夜主题 | 深蓝暗色调 | 夜间模式、专业报告 |
| 🤖 赛博朋克 | 霓虹紫粉配色 | 科技感展示、未来主题 |
| 💡 霓虹主题 | 亮绿粉红活力 | 年轻化、活力展示 |
| 🌌 极光主题 | 柔和青绿色调 | 自然、清新风格 |
| 🌊 海洋主题 | 深蓝海洋风格 | 数据可视化、企业应用 |
| 🌅 日落主题 | 温暖橙色渐变 | 温馨、友好场景 |
| 🌲 森林主题 | 自然绿色调 | 环保、健康主题 |

## 💻 开发者使用

### 基础用法

```vue
<template>
  <ChartThemeSwitcher />
</template>

<script setup>
import ChartThemeSwitcher from '@/components/ChartThemeSwitcher/index.vue'
</script>
```

### 获取当前主题

```javascript
import { useAppStore } from '@/store/modules/app'
import { getChartTheme } from '@/config/echartsThemes'

const appStore = useAppStore()
const themeName = appStore.getChartTheme
const theme = getChartTheme(themeName)

console.log(theme.displayName)  // "默认主题"
console.log(theme.colors)      // 颜色数组
```

### 切换主题

```javascript
import { useAppStore } from '@/store/modules/app'

const appStore = useAppStore()

// 切换到赛博朋克主题
appStore.setChartTheme('cyberpunk')

// 切换到森林主题
appStore.setChartTheme('forest')
```

### 监听主题变化

```javascript
import { onMounted, onUnmounted } from 'vue'

onMounted(() => {
  const handleThemeChange = (e) => {
    console.log('主题已切换:', e.detail.themeName)
  }

  window.addEventListener('chart-theme-change', handleThemeChange)
})

onUnmounted(() => {
  window.removeEventListener('chart-theme-change', handleThemeChange)
})
```

## 🎯 为图表添加主题支持

### 方式 1：使用 LazyChart（推荐，已自动支持）

所有使用 `LazyChart` 包裹的图表都自动支持主题切换：

```vue
<template>
  <div class="chart-card">
    <LazyChart :component="MyChart" />
  </div>
</template>
```

### 方式 2：手动应用主题

```vue
<template>
  <VChart :option="themedOption" />
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useAppStore } from '@/store/modules/app'
import { getChartTheme, applyThemeToOption } from '@/config/echartsThemes'

const appStore = useAppStore()

const baseOption = ref({
  // 你的图表配置
  title: { text: '我的图表' },
  series: [...]
})

const themedOption = ref(null)

const applyTheme = () => {
  const theme = getChartTheme(appStore.getChartTheme)
  themedOption.value = applyThemeToOption(baseOption.value, theme)
}

onMounted(() => {
  applyTheme()
  window.addEventListener('chart-theme-change', applyTheme)
})

onUnmounted(() => {
  window.removeEventListener('chart-theme-change', applyTheme)
})
</script>
```

## 🎨 自定义主题

### 添加新主题

在 `src/config/echartsThemes.ts` 中添加：

```typescript
export const echartsThemes = {
  // ... 现有主题
  myTheme: {
    name: 'myTheme',
    displayName: '我的主题',
    backgroundColor: 'transparent',
    titleColor: '#ffffff',
    subtitleColor: 'rgba(255, 255, 255, 0.6)',
    textColor: 'rgba(255, 255, 255, 0.7)',
    axisLineColor: 'rgba(255, 255, 255, 0.2)',
    splitLineColor: 'rgba(255, 255, 255, 0.08)',
    tooltipBg: 'rgba(15, 15, 35, 0.95)',
    tooltipBorder: 'rgba(102, 126, 234, 0.5)',
    colors: [
      '#667eea',
      '#764ba2',
      '#f093fb',
      '#4facfe',
      '#43e97b',
      '#fa709a',
      '#fee140',
      '#ff9f43'
    ]
  }
}
```

## 📚 核心文件说明

| 文件 | 说明 |
|------|------|
| `src/config/echartsThemes.ts` | 主题配置定义 |
| `src/components/ChartThemeSwitcher/` | 主题切换器 UI 组件 |
| `src/components/LazyChart.vue` | 懒加载图表组件（支持主题） |
| `src/composables/useChartTheme.ts` | 主题 composable 工具 |
| `src/utils/chartThemeManager.ts` | 主题管理器 |
| `src/store/modules/app.ts` | 状态管理（chartTheme） |

## 🔧 高级用法

### 获取所有主题

```javascript
import { getAllChartThemes } from '@/config/echartsThemes'

const allThemes = getAllChartThemes()
console.log(allThemes) // 所有主题对象数组
```

### 应用主题到配置

```javascript
import { getChartTheme, applyThemeToOption } from '@/config/echartsThemes'

const myOption = { /* ECharts 配置 */ }
const theme = getChartTheme('dark')
const themedOption = applyThemeToOption(myOption, theme)
```

## 📊 项目兼容性

- ✅ 支持所有 46 个图表组件
- ✅ 兼容 Vue 3 + ECharts 6
- ✅ 支持 vue-echarts
- ✅ 支持懒加载
- ✅ 支持响应式布局

## ❓ 常见问题

**Q: 主题切换后图表没有更新？**
A: 确保图表组件使用了 `LazyChart` 包裹，或者手动监听了 `chart-theme-change` 事件。

**Q: 如何自定义主题切换器的样式？**
A: 直接修改 `src/components/ChartThemeSwitcher/index.vue` 的 `<style>` 部分。

**Q: 主题会保存吗？**
A: 是的，主题选择会保存在 localStorage 中，刷新页面后会恢复。

**Q: 能否在运行时动态添加主题？**
A: 可以，直接修改 `echartsThemes` 对象即可，主题切换器会自动更新。

## 🎉 总结

现在你有了一个强大的图表主题切换系统！

- **用户**：一键切换所有图表的配色方案
- **开发者**：零代码侵入，轻松支持所有图表
- **扩展性**：轻松添加新主题，完全自定义

享受多彩的图表世界！🌈
